<%= form_with(model: sniper, local: true) do |form| %>
  <% if sniper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sniper.errors.count, "error") %> prohibited this sniper from being saved:</h2>

      <ul>
      <% sniper.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language %>
  </div>

  <div class="field">
    <%= form.label :contents %>
    <%= form.text_field :contents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
